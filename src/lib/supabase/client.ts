// Re-export the Lovable Cloud–managed Supabase client so imports from
// `@/lib/supabase/client` resolve to the same singleton used elsewhere.
export { supabase } from "@/integrations/supabase/client";
